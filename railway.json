{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @taskra/types build && pnpm --filter @taskra/sdk build && pnpm --filter taskra-api build && pnpm --filter taskra-agents-engine build",
    "watchPatterns": [
      "apps/api/src/**",
      "apps/agents/src/**",
      "packages/**"
    ]
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 5,
    "startCommand": "pnpm --filter taskra-api start"
  }
}
